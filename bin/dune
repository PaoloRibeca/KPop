(executables
 (public_names Parallel KPopCount KPopDB CovidDB)
 (names Parallel KPopCount KPopDB CovidDB)
 (libraries str unix BiOCamLib)
 (ocamlopt_flags (:standard -O3 -ccopt -O3 -ccopt -static)))

(install
 (section bin)
 (files
  (Parallel.exe as Parallel)
  (KPopCount.exe as KPopCount)
  (KPopDB.exe as KPopDB)
  (CovidDB.exe as CovidDB)))
